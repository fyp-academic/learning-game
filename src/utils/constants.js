export const WIN_STEPS = 10;
export const MAX_SHIFT_PERCENTAGE = 0.45; // pull video nearly halfway so team exits view
export const SIMULTANEOUS_WINDOW_MS = 240;
export const TEAM_LOCK_MS = 160;
export const ROUND_SECONDS = 5 * 60; // 5 minutes
export const VIDEO_SCALE = 1.05;

// If your video “center marker” isn’t perfectly centered at pullPosition=0,
// tweak this a little (in px): negative shifts left, positive shifts right.
export const BASE_OFFSET_PX = 0;

export const DEFAULT_MATH_OPS = ["+", "-", "×", "÷"];
